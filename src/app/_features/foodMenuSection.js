import { Profile } from "../_components/profile";

export const FoodMenuSection = () => {
  return (
    <div className="w-[80%] border border-black mt-10 flex flex-col items-end mb-20 justify-between">
      <Profile />
    </div>
  );
};
